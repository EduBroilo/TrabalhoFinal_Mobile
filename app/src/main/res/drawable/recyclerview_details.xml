<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="4dp"
        android:color="@color/white" /> <!-- Border color -->
    <corners android:radius="8dp" /> <!-- Optional rounded corners -->
</shape>